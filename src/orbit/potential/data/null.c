/*
 *  a Null .o file - keeps some non-gnu makefiles simple 
 */

/*CTEX
 *
 *  This potential has no other meaning other than to fool the compiler.
 *  It has no associated potential, thus the usual potname, potpars,potfile
 *  will have no meaning. Use the {\bf zero} potential if you want a real 
 *  potential with zero values.
 */

static int potential_fool_compiler = 0;
