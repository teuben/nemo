
A word count (wc) will be performed on these files upon unpacking. 
It should return no errors. 

To compile and install the FITSView programs, type

$ make all install

You should see the following:

cc -O  -sun4 -c  ql.c
cc -O  -sun4 -c  loadimage.c
cc -O  -sun4 -c  fits.c
cc -O  -sun4 -c  rainbow.c
cc -O  -sun4 -c  writeTGA.c
cc ql.o loadimage.o fits.o rainbow.o writeTGA.o -lsuntool -lsunwindow -lpixrect -lm -o ql
cc -O  -sun4 -c  rgb.c
cc rgb.o loadimage.o fits.o -lsuntool -lsunwindow -lpixrect -lm -o rgb
cc -O  -sun4 -c  rgb24.c
cc rgb24.o loadimage.o fits.o writeTGA.o -lsuntool -lsunwindow -lpixrect -lm -o rgb24
cc -O  -sun4 -c  movie.c
cc movie.o loadimage.o fits.o rainbow.o -lsuntool -lsunwindow -lpixrect -lm -o movie
install ql ../../../bin
install rgb  ../../../bin
install movie ../../../bin
install rgb24 ../../../bin


They are installed in /usr/comb/bin (which should be in your $PATH).

Now you're ready to go. Documentation is in FITSView.doc. Happy viewing!

ERRATUM 8/15/90:

The SunView call pr_putlut() is not supported on 8-bit displays.
If a compile error is generated by this call in ql.c and movie.c, 
just comment out the offending line.
