CREATE TABLE eps (name text unique, gas real, halo real, disk real, bulge real, stars real);                                                       
CREATE TABLE info (name text unique, type text, dir text, base text);                                                                              
CREATE TABLE nemorange (name text unique, total text, disk text, bulge text, halo text, halo2 text, gas text, bndry text, stars text);
.exit
.quit
