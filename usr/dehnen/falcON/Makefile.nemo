#
#

BINS = addgravity getgravity gyrfalcON lagrange_radii \
        manipulate mkdehnen mkking mkplum scale_eps snapstack \
 	symmetrize TestGrav

nemo_bins: $(BINS)
	make 
	(cd $(HOS); cp $(BINS) $(NEMOBIN))
	cp $(LIB)/lib*.so $(NEMOLIB)
