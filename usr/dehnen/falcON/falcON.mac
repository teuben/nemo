error 2		# force error (prop to) of (p,theta)
		set $0 = ($1+1) * $2**($1+2) / (1-$2)**2

cpu 2		# CPU time (prop to) of (p,theta)
		set $0 = ($1/$2)**3

plot_p 1	set th=0.01,0.99,0.01
		set le=lg(error($1,th))
		set lc=lg(cpu($1,th))
		connect le lc

plots 2		do i=$1,$2 {
			plot_p $i
		}
